require('dotenv').config();

module.exports = {
  HOST: "localhost",   // Database host (usually localhost for development)
  USER: "root",        // MySQL username (change this to your MySQL username)
  PASSWORD: "",        // MySQL password (change this to your MySQL password)
  DB: "SM_db",         // Database name (change this to your actual DB name)
};

